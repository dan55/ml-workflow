$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineDeployment.schema.json
name: ifear-deploy-ugh
endpoint_name: ifear-endpoint
model:
  azureml:ifear-registered-model:2
  # name: model
  # version: 2
  # path: ./model
  # type: mlflow_model
  # name: azureml:ifear-registered-model:3
# version: latest
# environment:
#   image: azureml:AzureML-sklearn-1.0-ubuntu20.04-py38-cpu:33
#   conda_file: sdl
# code_configuration:
#   code: ./model/
#   scoring_script: score.py
instance_type: Standard_DS2_v2
instance_count: 1
